{
  "name": "steam-notifier-worker",
  "version": "1.0.0",
  "description": "Steam Notifier – Cloudflare Worker (migrated from Practice 1)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.93.0"
  }
}
